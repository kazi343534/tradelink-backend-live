{
  "name": "tradelink-platform",
  "private": true,
  "version": "1.0.0",
  "description": "TradeLink - root wrapper that delegates build/start to backend/",
  "scripts": {
    "build": "cd backend && npm install --include=dev && npm run build",
    "start": "cd backend && npm install --omit=dev --no-audit --no-fund && node dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "multer": "^2.2.0",
    "pg": "^8.13.1",
    "zod": "^3.24.1"
  }
}
